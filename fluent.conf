<source>
  @type  forward
  port  24224
</source>

<match *.**>
  type redis_store
  host "#{ENV['REDIS_HOST']}"
  port "#{ENV['REDIS_PORT']}"
  key "#{ENV['FLUENTD_CHANNEL']}"
  store_type publish
</match>
